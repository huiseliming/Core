#include "Logger.h"
#include <cassert>
int main()
{
	//std::unordered_map<FLogMessage, std::list<time_t>> LogHashMap;
	//LogHashMap.insert(std::make_pair<>(FLogMessage{0, std::this_thread::get_id(), ELogLevel::kInfo, "???"}, 0));
	//assert(LogHashMap.find(FLogMessage{ 0, std::this_thread::get_id(), ELogLevel::kInfo, "???" }) != LogHashMap.end());
	//CLogger log;
	//std::vector<std::thread> ts;
	//for (size_t i = 0; i < 32; i++)
	//{
	//	ts.push_back(std::thread{
	//		[&]{
	//			for (size_t i = 0; i < 100; i++)
	//			{
	//				log.Log(ELogLevel::kInfo, std::to_string(i));
	//			}
	//		}
	//	});
	//}
	//for (size_t i = 0; i < 32; i++)
	//{
	//	ts[i].join();
	//}
}




